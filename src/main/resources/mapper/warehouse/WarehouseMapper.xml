<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swpu.equipment.warehouse.mapper.WarehouseMapper">

    <resultMap id="WarehouseVOResultMap" type="com.swpu.equipment.warehouse.entity.WarehouseVO">
        <id property="id" column="id"/>
        <result property="warehouseName" column="warehouse_name"/>
        <result property="warehouseLocation" column="warehouse_location"/>
        <result property="warehouseManagerId" column="warehouse_manager_id"/>
        <result property="warehouseManagerName" column="warehouse_manager_name"/>
        <result property="phone" column="phone"/>
        <result property="description" column="description"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="getWarehouseList" resultMap="WarehouseVOResultMap">
        SELECT 
            w.id,
            w.warehouse_name,
            w.warehouse_location,
            w.warehouse_manager_id,
            u.real_name AS warehouse_manager_name,
            w.phone,
            w.description,
            w.create_time
        FROM warehouse w
        LEFT JOIN sys_user u ON w.warehouse_manager_id = u.id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (w.warehouse_name LIKE CONCAT('%', #{keyword}, '%')
                     OR w.warehouse_location LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY w.create_time DESC
    </select>
    <!--
    获取管理员列表 -->
    <select id="getManagerOptions" resultType="map">
        SELECT u.id, u.real_name AS label
        FROM sys_user u
        WHERE u.real_name IS NOT NULL AND u.real_name != ''
          AND u.role = 'admin'
        ORDER BY u.real_name
    </select>

</mapper>
